<figure class="figure">
  <img
    src="{{ include.path | relative_url }}"
    alt="{{ include.alt | default: include.title | default: '' }}"
    loading="{{ include.loading | default: 'lazy' }}"
  >
  {% if include.caption %}
    <figcaption>{{ include.caption }}</figcaption>
  {% endif %}
</figure>
